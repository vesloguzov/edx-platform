---
metadata:
    display_name: Задача с использованием Javascript
    markdown: !!null
    showanswer: never
data: |
      <problem>
      <p>
      В этих задачах необходима программа на JavaScript.
      Конструктор вставляет программу в IFrame, и студенты могут
      взаимодействовать с ней. Оценивание работы студентов осуществляется с помощью
      JavaScript (и немного Python) и интегрируется в общую систему оценивания.
      </p>
      <p>
      Задачи, которые вы создаете, должны использовать HTML, JavaScript и CSS.
      </p>

      <p>После добавления задачи не забудьте зайти в <strong>«Настройки»</strong>, чтобы изменить отображаемое название компонента и другие опции.</p>
      <p>Вы можете использовать следующую задачу в качестве примера.</p>

          <customresponse cfn="check_function">
      <script type="loncapa/python">
      <![CDATA[
      import json
      def check_function(e, ans):
        """
        "response" is a dictionary that contains two keys, "answer" and "state".
        The value of "answer" is the JSON string that "getGrade" returns.
        The value of "state" is the JSON string that "getState" returns.
        Clicking either "Submit" or "Save" registers the current state.
        """
        response = json.loads(ans)

        # You can use the value of the answer key to grade:
        answer = json.loads(response["answer"])
        return answer == "correct"

        # Or you can use the value of the state key to grade:
        """
        state = json.loads(response["state"])
        return state["selectedChoice"] == "correct"
        """
      ]]>
      </script>
            <p>Данный абзац текста отображается перед IFrame.</p>
            <jsinput
              gradefn="JSInputDemo.getGrade"
              get_statefn="JSInputDemo.getState"
              set_statefn="JSInputDemo.setState"
              initial_state='{"selectedChoice": "incorrect1", "availableChoices": ["incorrect1", "correct", "incorrect2"]}'
              width="600"
              height="100"
              html_file="https://files.edx.org/custom-js-example/jsinput_example.html"
              title="Dropdown with Dynamic Text"
              sop="false"/>
          </customresponse>
      </problem>
